const name="Satya"
const repo=50;
console.log(`hello my name is ${name} and my repo count is ${repo}`);

const n=new String('Satyabrata');
console.log(n.length);

console.log(n.charAt(2));
console.log(n.indexOf('t'));

